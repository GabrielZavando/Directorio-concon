# Specboot framework is published to GitHub Packages (private scope).
# Authentication is machine-local: set the token in ~/.npmrc (never commit it):
#   //npm.pkg.github.com/:_authToken=ghp_xxx  (PAT with read:packages)
@gabrielzavando:registry=https://npm.pkg.github.com
