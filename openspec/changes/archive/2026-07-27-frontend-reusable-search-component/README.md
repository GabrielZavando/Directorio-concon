# frontend-reusable-search-component

Extract reusable search-bar component with own logic, fed by canonical Concon categorias/barrios JSON (DIP port + local impl + future remote fallback contract). Refactor home hero + directorio placeholder to consume it.
