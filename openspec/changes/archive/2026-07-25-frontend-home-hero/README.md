# frontend-home-hero

Add Hero section to home page with background image (panoramica-concon.jpg) and search form (text + categoria + ubicacion). Dumb hero + smart wrapper that navigates to /directorio with query params. Categorias/barrios hardcoded in smart component (dummy MVP) until categorias/barrios backend modules are implemented.
