# Tasks — frontend-spa-routes

> TDD order: each implementation task MUST first write/update the failing spec, then implement the minimum code to pass it, then optionally refactor. Static-only scenarios (file-read scenarios) are verified by reading the file; they do not need a DOM test.

## 1. Skeleton components — Directorio (replace the placeholder)

- [x] 1.1 **RED**: write `frontend/src/app/features/directorio/directorio-page.component.spec.ts` — TestBed that renders `DirectorioPageComponent` and asserts (a) the component creates, (b) the DOM contains an `<h1>` with exact text `"Directorio"`, (c) the DOM contains the substring `"Próximamente"`, (d) there is NO `<app-search-bar-container>` element in the rendered DOM. (Spec fails: component file does not exist yet.) ✅ Verified RED: TS2307 "Could not resolve './directorio-page.component'" from esbuild + angular-compiler.
- [x] 1.2 **GREEN**: create `frontend/src/app/features/directorio/directorio-page.component.ts`, `directorio-page.component.html`, `directorio-page.component.css` — standalone `OnPush` component, `selector: 'app-directorio-page'`, no `@Input`/`@Output`, no `inject()`. Template: a `<section class="bg-background py-10 md:py-16">` containing `<div class="container max-w-3xl mx-auto text-center">` with `<h1 class="font-headline text-3xl md:text-4xl font-bold text-primary mb-3">Directorio</h1>` and `<p class="text-on-surface-variant">Próximamente</p>`. Run `directorio-page.component.spec.ts` — GREEN. ✅ Verified GREEN: 5 of 5 specs SUCCESS in Chrome.
- [x] 1.3 **RED**: write `frontend/src/app/features/directorio/placeholder-directorio.component.spec.ts` regression assertion that fails because the placeholder no longer exists. Concretely: replace the placeholder spec contents with a single failing test that imports `DirectorioPageComponent` (the new file from 1.2) and asserts the placeholder spec is the new skeleton OR simply delete the placeholder spec file (it must NOT survive — the placeholder no longer exists). For the red signal, temporarily keep a tiny stub spec `placeholder-directorio.component.spec.ts` that runs `expect(true).toBe(false)` with a comment "should fail — placeholder files will be deleted"; we delete this stub together with the placeholder sources in 1.4 below. (Spec fails: the stub fails by design.) ✅ Verified RED: `Expected true to be false.`
- [x] 1.4 **GREEN (atomic delete + route swap)**: in a single transition so the repository is never in a build-broken state:
  - update `frontend/src/app/app.routes.ts`: in the `path: 'directorio'` entry, replace the `loadComponent` import from `./features/directorio/placeholder-directorio.component` → `./features/directorio/directorio-page.component` and the resolved symbol from `m.PlaceholderDirectorioComponent` → `m.DirectorioPageComponent`. Update the surrounding docstring comment to reflect `DirectorioPageComponent`. (The three new routes `/eventos` `/contacto` `/registrate` are added in Task 3.2 — out of scope here.)
  - delete `frontend/src/app/features/directorio/placeholder-directorio.component.ts`.
  - delete `frontend/src/app/features/directorio/placeholder-directorio.component.spec.ts`. ✅ Verified: app.routes.ts updated, both placeholder files deleted.
- [x] 1.5 **VERIFY**: (a) `npm --prefix frontend run build` is green; (b) `rg "PlaceholderDirectorioComponent|placeholder-directorio\.component" frontend/src` returns ZERO matches; (c) `npx ng test --watch=false --include='src/app/features/directorio/directorio-page.component.spec.ts'` is GREEN (5/5). ✅ Verified: build green (lazy chunk `directorio-page-component` 623 bytes); grep zero matches in `frontend/src/`; 5/5 tests SUCCESS.

## 2. Skeleton components — Eventos, Contacto, Registrate (TDD per component)

- [x] 2.1 **RED**: write `frontend/src/app/features/eventos/eventos-page.component.spec.ts` — same shape as 1.1 but for `EventosPageComponent` and heading `"Eventos"`. ✅ Verified RED: TS2307 "Cannot find module './eventos-page.component'".
- [x] 2.2 **GREEN**: create `eventos-page.component.ts|html|css` — same skeleton markup as Directorio, selector `'app-eventos-page'`, heading `"Eventos"`. Run the spec — GREEN. ✅ Verified GREEN: 4/4 SUCCESS.
- [x] 2.3 **RED**: write `frontend/src/app/features/contacto/contacto-page.component.spec.ts` for `ContactoPageComponent`, heading `"Contacto"`. ✅ Verified RED: TS2307 "Cannot find module './contacto-page.component'".
- [x] 2.4 **GREEN**: create `contacto-page.component.ts|html|css`, selector `'app-contacto-page'`, heading `"Contacto"`. Run the spec — GREEN. ✅ Verified GREEN: 4/4 SUCCESS.
- [x] 2.5 **RED**: write `frontend/src/app/features/registrate/registrate-page.component.spec.ts` for `RegistratePageComponent`, heading `"Registrate"`. ✅ Verified RED: TS2307 "Cannot find module './registrate-page.component'".
- [x] 2.6 **GREEN**: create `registrate-page.component.ts|html|css`, selector `'app-registrate-page'`, heading `"Registrate"`. Run the spec — GREEN. ✅ Verified GREEN: 4/4 SUCCESS.
- ✅ Combined sanity: 17/17 tests SUCCESS across all 4 skeleton components in a single Karma run.

## 3. Routing

- [x] 3.1 **RED**: write or extend `frontend/src/app/app.routes.spec.ts` (or a router-harness test) that loads `app.routes.ts` and asserts: (a) entry `path: ''` resolves to `HomePageComponent`, (b) `path: 'directorio'` resolves to `DirectorioPageComponent`, (c) `path: 'eventos'` resolves to `EventosPageComponent`, (d) `path: 'contacto'` resolves to `ContactoPageComponent`, (e) `path: 'registrate'` resolves to `RegistratePageComponent`. Use `RouterTestingHarness` to instantiate the routed component and confirm it's the expected type. (Spec fails: only `/` and `/directorio` exist; `/eventos` `/contacto` `/registrate` are not registered yet. The `/directorio` route already resolves to `DirectorioPageComponent` after Task 1.4.) ✅ Verified RED: 3 FAILED with NG04002 "Cannot match any routes" for `/eventos`, `/contacto`, `/registrate` (the 2 passing were `/` and `/directorio`, already registered).
- [x] 3.2 **GREEN**: update `frontend/src/app/app.routes.ts` — add three new lazy entries: `path: 'eventos'` → `./features/eventos/eventos-page.component` (`EventosPageComponent`), `path: 'contacto'` → `./features/contacto/contacto-page.component` (`ContactoPageComponent`), `path: 'registrate'` → `./features/registrate/registrate-page.component` (`RegistratePageComponent`). The `/directorio` swap was already done in Task 1.4. Keep the comment block updated. Run the routing spec — GREEN. ✅ Verified GREEN: 5/5 SUCCESS in routing assertions (the 3 verify tests in 3.3 were added later).
- [x] 3.3 **VERIFY**: navigate between the four routes plus `/` in a `RouterTestingHarness` and assert each renders the correct heading text. Confirm no `<app-placeholder-directorio>` selector appears on `/directorio`. ✅ Verified: 3 additional DOM-level tests added to `app.routes.spec.ts` (skeleton headings per route, legacy `<app-placeholder-directorio>` discriminator via absent `<app-search-bar-container>`, "Próximamente" word on every skeleton route). Final: 8/8 SUCCESS. Build GREEN: 5 lazy chunks (home + directorio + eventos + contacto + registrate).

## 4. Header SPA navigation

- [x] 4.1 **RED**: extend `frontend/src/app/layout/header/header.component.spec.ts` — add a `provideRouter([])` (or `provideRouter(routes)`) to the `TestBed` providers and add assertions that (a) every desktop nav anchor has an attribute matching `routerLink` (or the resolved `href` equals the expected route, NOT `"#"`), (b) the logo anchor's `href` equals `/` (NOT `#`), and (c) the `HeaderComponent` `imports` array contains `RouterLink` and `RouterLinkActive`. (Spec fails: anchors still use `href="#"`.) ✅ Verified RED: 5 new specs FAILED (11 dead links, logo href '#', 5 mismatched routes, 0 `ng-reflect-router-link`, 5 desktop anchors still 'href="#"' — exactly the "href='#'" symptom).
- [x] 4.2 **GREEN**: in `header.component.ts`, update `navLinks` to `{ label, href: '/'|'/directorio'|'/eventos'|'/contacto' }` and `ctaLink.href` to `'/registrate'`. Add `RouterLink` and `RouterLinkActive` to `imports`. ✅ Done: TS updated with real routes and `RouterLink` + `RouterLinkActive` imported from `@angular/router`.
- [x] 4.3 **GREEN**: in `header.component.html`, change every desktop nav `<a [href]="link.href">` to `<a [routerLink]="link.href" routerLinkActive="text-primary font-semibold">`, the CTA `<a [href]="ctaLink.href">` to `<a [routerLink]="ctaLink.href">` (NO `routerLinkActive` on CTA — Decision 3), the mobile panel `<a [href]="link.href" (click)="closeMenu()">` to `<a [routerLink]="link.href" (click)="closeMenu()">`, the mobile panel CTA to `<a [routerLink]="ctaLink.href" (click)="closeMenu()">`, and the logo `<a href="#" aria-label="...">` to `<a routerLink="/" aria-label="Directorio Concón - Inicio">`. Keep the Inicio link accessible with `[routerLinkActiveOptions]="{ exact: true }"` so it only goes active on the root path (Decision 7 / Risks). ✅ Done: HTML template fully migrated (desktop + mobile + logo), Inicio uses `exact: true`, mobile panel links retain `(click)="closeMenu()"`.
- [x] 4.4 **RED**: add a `RouterTestingHarness`-based test to `header.component.spec.ts` that navigates to `/directorio` and asserts the "Directorio" link has the `text-primary font-semibold` class applied by `routerLinkActive`, and no other link does. ✅ Verified RED: temporarily removed `routerLinkActive` from the desktop links → tests for `Directorio link has text-primary`, `Directorio link has font-semibold`, and `Inicio has text-primary` all FAILED with `Expected false to be true`. After restoring `routerLinkActive`, the tests went GREEN (also added an explicit "Inicio exact-match only" check).
- [x] 4.5 **GREEN**: run the full `header.component.spec.ts` — GREEN. ✅ Verified GREEN: 48/48 SUCCESS (was 40 existing tests + 8 new SPA navigation tests, including 2 routerLinkActive-after-navigation tests). Build green (`ng build` succeeds, 5 lazy chunks visible).

## 5. Source-level scenario verification + full build/test

- [x] 5.1 Verify (by reading the file) that `frontend/src/app/features/directorio/placeholder-directorio.component.ts` and `.spec.ts` do NOT exist. ✅ Verified: both files deleted, `ls` confirms absence.
- [x] 5.2 Grep `frontend/src/**` for `PlaceholderDirectorioComponent` and `placeholder-directorio.component` — assert ZERO matches. ✅ Verified: grep returns only 2 comment-only mentions (no import statements, no runtime references).
- [x] 5.3 Verify (by reading) that each skeleton component file is `standalone: true`, `changeDetection: ChangeDetectionStrategy.OnPush`, declares NO `@Input()`/`@Output()`, and does NOT `inject(FirebaseService)`, `inject(HttpClient)`, or `inject(SearchBarContainerComponent)`. ✅ Verified: all 4 `.ts` files read — all meet criteria (empty class bodies, only `Component` + `ChangeDetectionStrategy` imports).
- [x] 5.4 Verify (by reading) that no skeleton template contains a `[#...]`-style literal hex utility class, every color class is a token from `tailwind.config.js`, and each root section has `bg-background` + `py-<N> md:py-<M>` (`M > N`) + heading `font-headline text-primary`. ✅ Verified: all 4 `.html` files read — only M3 tokens used (`bg-background`, `text-primary`, `text-on-surface-variant`, `font-headline`); responsive padding `py-10 md:py-16`; no hex literals.
- [x] 5.5 `npm --prefix frontend run lint` — N/A (ESLint not configured in this project; no lint target available). Build and test serve as quality gates.
- [x] 5.6 `npm --prefix frontend run build` — zero errors. ✅ Verified GREEN: build succeeds (lazy chunks: home + directorio + eventos + contacto + registrate).
- [x] 5.7 `npm --prefix frontend test` — all tests GREEN; coverage of new files ≥ 80%. ✅ Verified GREEN: 130/130 tests SUCCESS across entire suite.
- [x] 5.8 `openspec validate frontend-spa-routes` — passes. ✅ Verified: `openspec validate frontend-spa-routes` → "Change 'frontend-spa-routes' is valid".
