# auth-usuarios

Firebase Auth JWT verification, RolesGuard, usuarios CRUD, and closure of the 3 authentication debts documented in roles-rename: places.usuarioId (anonymous→UID), eventos.usuarioId (x-usuario-id header→JWT), solicitudes.revisadoPor (no validation→admin-only)
